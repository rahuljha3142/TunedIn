```markdown
# TunedIn - Music Player Frontend 🎧

A sleek, responsive music player interface built with vanilla JavaScript. It fetches music data from the TunedIn Backend API and provides a seamless listening experience.

## ✨ Features
- **Dynamic Playback**: Real-time progress bar, volume control, and play/pause functionality.
- **Search & Filter**: Instant search by song title across the entire library.
- **Theme Support**: Includes a dark mode toggle for comfortable nighttime listening.
- **Environment Awareness**: Automatically switches between localhost (port 5000) and your production Render URL.
- **Keyboard Shortcuts**: Space for Play/Pause, Arrow keys for navigation.

## 🛠️ Tech Stack
- HTML5 / CSS3
- Vanilla JavaScript (ES6+)

## 🌐 Deployment
This frontend is optimized for **Vercel**. 
1. Ensure `index.html` is at the root of the repository.
2. Update the `API_BASE_URL` in `script.js` to point to your live Render backend URL before pushing.

## 🚀 Quick Start
1. Ensure the Backend is running.
2. Open `index.html` via a local server (like VS Code Live Server).